---
name: markdown-pointer-skill
description: Check package-local Markdown pointer handling at block boundaries.
---

# Markdown Pointer Skill

This quoted list shows literal syntax:

> - `literal example
>   [example only](references/example-only.md) closing delimiter`

This nested list requires its linked guide:

- > `unclosed example
    - Read [the live guide](references/live-guide.md). `closing delimiter
